#!/usr/bin/env node

// CLI Entry point
try {
  require("./task-cli.js");
} catch (error) {
  console.error(`[FATAL] Failed to start CLI: ${error.message}`);
  console.error(error.stack);
  process.exit(1);
}
