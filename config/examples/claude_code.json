{
  "mcpServers": {
    "agentic-control-framework": {
      "command": "node",
      "args": ["./bin/agentic-control-framework-mcp", "--workspaceRoot", "${cwd}"],
      "env": {
        "WORKSPACE_ROOT": "${cwd}"
      }
    }
  }
}

